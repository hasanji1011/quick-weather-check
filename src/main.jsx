import { StrictMode, useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import './styles.css';
const conditions={0:['Clear sky','☀️'],1:['Mainly clear','🌤️'],2:['Partly cloudy','⛅'],3:['Overcast','☁️'],45:['Foggy','🌫️'],48:['Foggy','🌫️'],51:['Light drizzle','🌦️'],53:['Drizzle','🌦️'],55:['Heavy drizzle','🌧️'],61:['Light rain','🌦️'],63:['Rain','🌧️'],65:['Heavy rain','🌧️'],71:['Light snow','🌨️'],73:['Snow','🌨️'],80:['Rain showers','🌦️'],81:['Rain showers','🌧️'],82:['Heavy showers','⛈️'],95:['Thunderstorm','⛈️'],96:['Thunderstorm with hail','⛈️'],99:['Thunderstorm with hail','⛈️']};
function App(){const[query,setQuery]=useState('Delhi'),[place,setPlace]=useState({name:'Delhi',country:'India',latitude:28.6139,longitude:77.209}),[weather,setWeather]=useState(null),[message,setMessage]=useState('Getting current weather…'),[loading,setLoading]=useState(false);useEffect(()=>{loadWeather(place)},[place]);async function loadWeather(location){setLoading(true);setMessage('Getting current weather…');try{const p=new URLSearchParams({latitude:location.latitude,longitude:location.longitude,current:'temperature_2m,relative_humidity_2m,apparent_temperature,weather_code,wind_speed_10m',timezone:'auto'}),r=await fetch(`https://api.open-meteo.com/v1/forecast?${p}`);if(!r.ok)throw Error();const{current}=await r.json();setWeather(current);setMessage('Updated just now')}catch{setMessage('Could not refresh conditions')}finally{setLoading(false)}}async function search(e){e.preventDefault();const city=query.trim();if(!city)return;setLoading(true);setMessage(`Finding ${city}…`);try{const r=await fetch(`https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(city)}&count=1&language=en&format=json`),d=await r.json();if(!d.results?.length)throw Error();const x=d.results[0];setPlace({name:x.name,country:x.country,latitude:x.latitude,longitude:x.longitude});setQuery(x.name)}catch{setMessage(`No city found for “${city}”.`);setLoading(false)}}const[condition,icon]=conditions[weather?.weather_code]||['Getting current weather…','☀️'],time=weather&&new Intl.DateTimeFormat('en-IN',{hour:'numeric',minute:'2-digit'}).format(new Date()),stat=(label,value)=><div className="stat"><span>{label}</span><strong>{value}</strong></div>,failed=message.startsWith('No city')||message.startsWith('Could not');return <main className="weather-card" aria-live="polite"><form className="search" onSubmit={search}><input aria-label="City name" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Enter a city"/><button disabled={loading}>{loading?'…':'Search'}</button></form><div className="topline"><span>Live conditions</span><time>{time||'Loading…'}</time></div><div className="location"><span className="pin">●</span><span>{place.name}, {place.country}</span></div><div className="reading"><div><div className="temperature">{weather?Math.round(weather.temperature_2m):'--'}<span className="degree">°</span></div><div className="condition">{weather?condition:'Getting current weather…'}</div></div><div className="icon">{icon}</div></div><section className="stats">{stat('Feels like',weather?`${Math.round(weather.apparent_temperature)}°`:'--°')}{stat('Humidity',weather?`${weather.relative_humidity_2m}%`:'--%')}{stat('Wind',weather?`${Math.round(weather.wind_speed_10m)} km/h`:'-- km/h')}</section><div className={`status${failed?' error':''}`}>{!failed&&<i className="dot"/>}<span>{message}</span></div></main>}
createRoot(document.getElementById('root')).render(<StrictMode><App/></StrictMode>);